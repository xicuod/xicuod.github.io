---
weight: 160
slug: linux-upload-download
title: Linux 上传和下载
---

## rz 和 sz 命令：文件传输

可以通过 rz 和 sz 命令进行文件传输，它们包含在 `lrzsz` 软件包中。

- `rz` 上传，使用远程终端工具的上传实现
- `sz 要下载的文件`下载，下载到远程终端工具的下载文件夹 (FinalShell 的是`桌面/fsdownload`)

需要在 FinalShell 等支持的远程终端环境中使用这两个命令。
