---
weight: 50
slug: java-identifier
title: Java 标识符
---

**标识符**是开发者给具体对象的自定义名称，如类名、方法名、变量名等。

## 标识符的命名规则

1. 不能包含不允许的字符，只能包含 `A-z`、`0-9`、`$` 和 `_`。错误示例：`Hello世界`
2. 不能以数字开头。错误示例：`123abc`
3. 不能是关键字。错误示例：`public`

## 标识符的命名规范

- 大驼峰式：`NewVar`，用于类名
- 小驼峰式：`newVar`，用于方法名、变量名
