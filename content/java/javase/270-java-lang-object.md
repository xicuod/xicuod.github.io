---
weight: 270
slug: java-lang-object
title: java.lang.Object
---

`java.lang.Object` 类的方法：

- `hashCode` 方法：返回基于该对象地址计算的哈希值
- `toString` 方法：返回表示对象内容的字符串
- `clone` 方法：克隆一个对象并返回
- `getClass` 方法：返回对象的实际类型
- `notify` 方法：多线程通讯
- `wait` 方法：多线程通讯
- `equals` 方法：返回是否为相同对象
