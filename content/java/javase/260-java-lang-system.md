---
weight: 260
slug: java-lang-system
title: java.lang.System
---

`java.lang.System` 类的静态方法：

- `long currentTimeMillis()` 返回当前时刻毫秒值
- `void arraycopy(Object src, int srcIndex, Object dest, int destPos, int length)` 将当前数组中指定数据拷贝到另一个数组中，会覆盖原数据
